# План: финальный спринт (ночь с 09.09 на 10.09, дедлайн 17:00)

> Автономный режим, подтверждён владельцем: ручные проверки заменены автопроверками
> (`pnpm lint && pnpm typecheck && pnpm test` + реальный запуск), каждая запись в DEVLOG
> помечается «автопроверка вместо ручной». Утром — ручной прогон по чеклисту §16 TZ.

## Остаток работ по PLAN.md

| Этап | Статус до ночи | Что делаем этой ночью |
|---|---|---|
| 8. UI | только index.vue-демо | Все 7 экранов FLOWS.md + API-эндпоинты под них |
| 9. MVP | нет | Страница MVP для идеи §1: обращение → классификация LLM → валидатор → сохранение → показ. Изолированный server route |
| 10. Доступы | нет | Роль по cookie/заголовку: owner / demo (read-only). Read-only middleware на мутации. `.env.example` |
| 11. Кейсы + матрица | нет | 3 кейса (полный/облегчённый ×2) как dataset + скрипт прогонов; тест-матрица в docs |
| 12. Документация | docs/ есть, в сервисе нет | Страница `/docs` в сервисе: рендерит docs/*.md + DEVLOG |
| 13. Деплой | нет | Dockerfile уже есть; docker-compose prod-профиль, инструкция DEPLOY.md, план скринкаста |

## Порядок

1. **Серверные API для UI** (не трогая существующие): idea detail/patch/archive, job controls
   (pause/resume/cancel/retry/priority), report, runs, latest job. Все через `apiError()`,
   validate→service→return.
2. **UI**: layout + 7 экранов по FLOWS.md, shadcn-vue, автообновление активных jobs.
3. **MVP-эндпоинт + страница** `/mvp`.
4. **Доступы**: middleware read-only для demo-режима, `.env.example`, SECURITY-раздел в docs.
5. **Кейсы**: датасет 200 обращений (fixture, помечен), скрипт прогона 3 кейсов, матрица.
6. **/docs-страница** из docs/*.md.
7. **Финал**: lint/typecheck/test/e2e, docker-compose prod, DEPLOY.md, план скринкаста, чеклист §16.

## Принятые допущения (фиксирую, чтобы утром легко откатить)

- Роли owner/demo — через cookie `role` + env `DEMO_PASSWORD` (простая авторизация, без JWT):
  минимум для разделения доступов по §11, честно описано как упрощение прототипа.
- Отчёт — из таблицы Reports (собирается fixture-шагом report_build из agent_outputs).
- Прогонный модуль (этап 6) уже сохраняет Runs/RunCalls/Calculations — UI читает их напрямую.
- MVP — отдельный page + server route, вызывающий те же реальные компоненты (llm + validator),
  помеченный в DEVLOG как отдельный изолированный сценарий (не в процессе воркера с ключами).
- Скринкаст — физически делает человек; я готовлю посекундный план.
